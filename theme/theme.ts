// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  // Your theme settings, for example:
  palette: {
    primary: {
      main: '#33dd33',
    },
    secondary: {
      main: '#19857b',
    },
    // Add more customizations here
  },
  // Other global settings like typography, breakpoints, etc.
});

export default theme;
